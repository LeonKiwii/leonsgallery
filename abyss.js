(()=>{const dialog=document.querySelector('dialog');dialog.addEventListener('cancel',e=>e.preventDefault());let elapsed=0,last=performance.now();function tick(now){if(!document.hidden&&!document.body.classList.contains("about-open")&&!document.querySelector(".menu-context[open]"))elapsed+=now-last;last=now;if(elapsed>=10000){dialog.showModal();dialog.querySelector('a').focus();}else requestAnimationFrame(tick);}const start=()=>{last=performance.now();requestAnimationFrame(tick);};if(document.fonts)document.fonts.ready.then(start);else start();})();
